export class Keg {
  constructor(public name: string, public brand: string, public alcoholContent: number, public flavor: string, public price: number, public pints: number) {}
  // name: string;
  // brand: string;
  // alcoholContent: number;
  // flavor: string;
  // price: number;
  // pints: number;
}
